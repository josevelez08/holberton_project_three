#include <stdio.h>
/**
	 * main - alfabeto mayusculas y minisculas
	 *
	 * Return: 0 always runs
	 */
	int main(void)
	{
		int i;
		

		for (i = 97; i < 123; ++i)
			{
			putchar (i);
			}	
		for (i = 65; i < 91; ++i)
			{
				putchar (i);
			}
	}
		return (0);
}
