#include <stdio.h>
/**
	 * main - alfabeto mayusculas y minisculas
	 *
	 * Return: 0 always runs
	 */
	int main(void)
	{
		int h;
				for (h = 97; h < 123; h++)
			{
			putchar (h);
			}
		for (h = 65; h < 91; h++)
			{
				putchar (h);
			}
	} putchar ('\n')
		return (0);
}
