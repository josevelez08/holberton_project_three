#include "holberton.h"
#include <stdio.h>

/**
 * print_sign - mayor o menor
 * @n: character to be checker
 * Return: 1 is true or 0 is false
 */

int print_sign(int n);

{
	
	if (n > 0)
	{
		return (1);
		print_sign (+)
	}
	else if (n == 0)
	{ 
		return
	}
}