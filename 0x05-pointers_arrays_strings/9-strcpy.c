#include "holberton.h"
#include <stdio.h>

/**
* _strcpy - check the code for Holberton School students.
* @dest: destino
* @src: fuente
* Return: Always 0.
*/
char *_strcpy(char *dest, char *src)
{
	char *p;

	p = dest;
while ((*dest++ = *src++))
{; }

return (p);
}
