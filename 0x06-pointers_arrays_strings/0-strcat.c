#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * i = tamaño
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	int i, l, j;

	while ( *(dest + i) != 0);
	{
		i = i + 1;
	}
	while ( *(dest + i) != 0);
	{
		i = i + 1;
	}
 return (dest);
}
